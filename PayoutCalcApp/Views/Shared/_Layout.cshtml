<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
<div class="container body-content">
    @RenderBody()       
</div>
@Scripts.Render("~/bundles/otf")
@Scripts.Render("~/bundles/bootstrap")
@RenderSection("scripts", required: false)
<script type="text/javascript">
    $("#SelectedBedTimeHours").on("change",
        function () {
            var v = $("#SelectedBedTimeHours").val();
            var va2 = $("#SelectedStartTimeHours").val();
            debugger;
            return false;
        });
    function checkErrorMessage(model) {
        debugger;
        if (model.ErrorMessage != undefined) {
            $("#errorMessage").html('<h5 class="text-danger">' + model.ErrorMessage + '</h5>');
        }
    }

    function resetErrorMessage() {
        debugger;
        $("#errorMessage").html("");
    }
</script>
</body>
</html>

